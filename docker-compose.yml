version: '3'

services:
  nginx:
    build: nginx
    ports:
      - 80:80
      - 443:443
    depends_on:
      - app


  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    expose:
      - "8000"
    ports:
      - 8000:8000
